/**
 * Placeholder — the Log view itself is T-058's scope, not this ticket's.
 * This exists only so the shared chrome's nav has a real route to land on
 * rather than a 404, per T-057's Scope ("build this shell now since Trends
 * ships first").
 */
export function LogPage() {
	return (
		<div className="page-body">
			<div className="empty-state">
				<div className="headline">Log view coming soon</div>
				<div className="sub">Built in T-058, against this app's shell.</div>
			</div>
		</div>
	);
}
