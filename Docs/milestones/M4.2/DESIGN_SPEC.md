# Design Spec — M4.2: Entity Detection & Linking

**Status:** PENDING — Visual gates unresolved. Do not implement CP-5, CP-6, or CP-7 until this file is complete.

Human planning session in progress. Visual spec will be filled in after the user resolves design questions in a separate chat session.

---

## §1 Inline Highlight States

_Pending. Will define:_
- Confirmed match: CSS properties (color, border-bottom style/color/opacity, hover state, text-shadow)
- Ambiguous match: CSS properties (different treatment from confirmed)
- Click target behavior for each state

---

## §2 Quick-Create Popover

_Pending. Will define:_
- Popover dimensions and positioning (anchor point relative to span)
- Type selector layout (chip buttons vs. radio vs. dropdown)
- Field layout and sizing
- After-creation animation / transition

---

## §3 Detected Entities Sidebar

_Pending. Will define:_
- Panel placement (in full editor and docked panel)
- Grouping and sort order
- Row layout (avatar size, name truncation, occurrence count placement)
- Click behavior (scroll to first occurrence, selection behavior)
- Empty state copy and visual

---

## §4 Dismissal UX

_Pending. Will define:_
- How the DM dismisses a false-positive highlight
- Visual affordance (hover reveal, right-click, other)
